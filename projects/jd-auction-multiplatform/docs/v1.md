# 1. 设计目标

本设计用于后续正式落地到 MySQL 数据库 `auction_data`。系统不再只服务京东拍卖，而是面向京东拍卖、阿里拍卖、e交易、产权交易平台等多来源数据统一存储。

核心目标：

- 支持多平台采集，不让其他平台硬套京东 `paimai_id`。
- 支持 AI 主提取，规则/API 只负责拿原文、验真、标准化和冲突检测。
- 支持字段追溯，每一个展示字段都能看到来源页面、标签页、原文片段和提取方式。
- 支持债权多户、知识产权多明细等一对多结构。
- 支持跨平台去重，避免同一资产在多个平台重复入库。
- 支持定时任务、增量采集、失败重试和质量审核。

## 2. 关键设计结论

### 2.1 主键设计

正式表使用内部主键 `item_id BIGINT AUTO_INCREMENT`。

平台自己的标的编号使用：

- `source_platform`：来源平台，如 `jd`、`ali`、`ejy365`、`cquae`等。
- `source_item_id`：平台内标的 ID，如京东 `310826968`。

唯一约束：

UNIQUE KEY uk_source_item (source_platform, source_item_id)

### 2.2 价格字段口径

主表只保留两类价格：

- `start_price_amount/display`：起拍价、挂牌价、转让底价。
- `final_price_amount/display`：采集这一刻的业务有效价，也就是页面展示时的“最终价/当前有效价”。

不再保留 `current_price_*` 字段。

`final_price` 的取值规则：

| 项目状态    | final_price 取值                   |
| ----------- | ---------------------------------- |
| 未开始      | 起拍价                             |
| 竞价中      | 平台实时当前价                     |
| 已成交      | 成交价或最后一笔有效出价           |
| 流拍/未成交 | 最后可见价；没有出价时回退起拍价   |
| 撤拍/中止   | 最后可见价；没有可见价时回退起拍价 |

同时使用 `price_basis` 记录价格依据：

- `start_price_fallback`
- `realtime_current_price`
- `latest_bid_price`
- `deal_price`
- `formula_fallback`
- `manual_review`

公式 `起拍价 + 出价次数 * 加价幅度` 只能作为兜底，不能优先使用。

### 2.3 展示值与原始值

主业务表不再同时保存“原始展示值”和“展示值”。

字段采用两段式：

- `*_amount`、`*_date`、`*_datetime`：标准化值，供 SQL 查询、排序、统计使用。
- `*_display`：页面展示值，尽量保留平台原始可读格式。

真正的原始证据放在 `field_extractions.source_excerpt`，不在主表重复保存。

### 2.4 评估价格及时间

评估价格必须来自明确证据，不能从比例、比较描述中误提取。

允许来源：

- 页面价格区明确写有 `评估价`、`市场价`、`参考价` 等，并且对应明确金额。
- 公告、详情、附件中明确写有 `评估价`、`评估价值`、`市场价值`、`评估基准日`。
- 评估报告附件中可解析出明确金额和日期。

禁止来源：

- “市场价 2 倍”“低于市场价”“约为评估价的多少比例”这类比较描述。
- 没有金额的“评估报告”“评估机构”字样。
- AI 推测出的价格。

如果没有明确金额，则 `assessment_price_amount` 和 `assessment_price_display` 必须为空。

### 2.5 处置方与处置机构

两者分开保存：

- `disposal_party`：真正有权处置资产的主体，如法院、银行、AMC、破产管理人、转让方。
- `disposal_agency`：服务机构、店铺、拍辅机构、律所、平台上传机构。

页面只有一个主体时，优先放入 `disposal_party`；如果接口同时存在 `orgName` 和 `shopName`，要分别保存并在证据表记录来源。

### 2.6 图片、视频、附件

文件和媒体不再只塞进一个大 JSON 字段。

统一进入 `item_resources`：

- `attachment`：公告、报告、清单、Excel、Word、PDF。
- `image`：现场图片、标的图片、车辆图片等。
- `video`：视频。

资源只保存 URL、文件名、类型、哈希、来源区块，不默认下载实体文件。

### 2.7 字段证据

所有字段提取结果都进入 `field_extractions`。

同一个字段可以有多条候选结果，例如：

- API 提取一条。
- HTML 规则提取一条。
- AI 提取一条。
- 标准化/验真后选中一条。

使用 `is_selected=1` 表示最终采用的值。冲突、缺失、被拒绝的结果也保留，方便 Viewer 展示原因。

### 2.8 去冗余口径

以下字段不再放在各资产特有表中：

- `disclosed_defects`：提升到 `auction_items`，因为土地、房地产、车辆、债权、设备等类型都会有公示瑕疵、风险提示或权利负担。
- `right_holder`：提升到 `auction_items`，表示权利人、所有权人、产权人；不是处置方，也不是债权人。知识产权逐项明细中的单项权利人仍保留在 `asset_ip_details.right_holder`。
- 图片摘要字段：不再使用 `site_images_summary`、`vehicle_images_summary`，现场图片、车辆图片、标的图片统一写入 `item_resources`，用 `resource_role` 区分。
- 附件和媒体数量：不在 `auction_items` 冗余保存，通过 `item_resources` 实时统计。
- `benchmark_date_display`：基准日只在业务表保留 `DATE` 类型；原始展示文本和证据片段进入 `field_extractions`。

债权汇总表中的 `principal_balance_display`、`interest_balance_display`、`claim_total_display` 保留，用于展示“万元、亿元、含罚息”等业务口径；逐户明细表不保留 `*_display`，逐户原文统一从 `field_extractions` 或 `source_excerpt` 追溯。

## 3. 表结构总览

| 表名                 | 作用                                                 |
| -------------------- | ---------------------------------------------------- |
| crawl_jobs           | 定时任务配置表，记录每天爬哪些平台、哪些类目、多少页 |
| crawl_job_runs       | 每次任务执行记录                                     |
| crawl_batches        | 每次采集批次记录，可由手动任务或定时任务产生         |
| crawl_queue          | 标的详情采集队列，支持断点续爬和失败重试             |
| auction_items        | 所有平台、所有资产类型的共有字段主表                 |
| raw_payloads         | 原始 JSON、HTML、正文、附件文本等归档表              |
| field_catalog        | 字段字典和中文备注                                   |
| field_extractions    | 字段提取证据表                                       |
| item_resources       | 附件、图片、视频资源表                               |
| asset_land           | 土地特有字段                                         |
| asset_real_estate    | 房地产特有字段                                       |
| asset_equipment      | 设备特有字段                                         |
| asset_vehicle        | 车辆特有字段                                         |
| asset_debt           | 债权汇总字段                                         |
| asset_debt_details   | 债权逐户明细                                         |
| asset_equity         | 股权特有字段                                         |
| asset_ip             | 知识产权汇总字段                                     |
| asset_ip_details     | 知识产权逐项明细                                     |
| asset_goods          | 物资产品特有字段                                     |
| asset_usufruct       | 用益物权特有字段                                     |
| asset_other          | 其他类型兜底字段                                     |
| asset_dedup_index    | 跨平台去重索引                                       |
| review_queue         | 人工审核队列                                         |
| data_quality_reports | 批次质量报告                                         |

## 4. 关系图

## 5. 核心表设计

### 5.1 auction_items：共有字段主表

这是 Viewer 和业务查询最常用的主表。每个平台每个标的一行。

| 字段                     | 类型          | 说明                                               |
| ------------------------ | ------------- | -------------------------------------------------- |
| item_id                  | BIGINT PK     | 系统内部标的 ID                                    |
| source_platform          | VARCHAR(50)   | 来源平台                                           |
| source_item_id           | VARCHAR(100)  | 平台原始标的 ID                                    |
| source_url               | VARCHAR(1000) | 标的页面 URL                                       |
| asset_group              | VARCHAR(50)   | 统一资产类型代码                                   |
| asset_group_label        | VARCHAR(50)   | 资产类型中文名                                     |
| source_category_id       | VARCHAR(100)  | 平台原始类目 ID                                    |
| source_category_name     | VARCHAR(200)  | 平台原始类目名称                                   |
| asset_type               | VARCHAR(200)  | 标的具体类型                                       |
| asset_location           | VARCHAR(1000) | 标的所在地                                         |
| project_status           | VARCHAR(50)   | 采集时项目状态                                     |
| project_status_basis     | VARCHAR(200)  | 状态判断依据                                       |
| auction_stage            | VARCHAR(100)  | 一拍、二拍、变卖、撤拍、中止等                     |
| bid_records_count        | INT           | 出价次数                                           |
| bid_records_json         | JSON          | 出价记录快照                                       |
| data_source              | VARCHAR(200)  | 数据来源名称                                       |
| project_name             | VARCHAR(1000) | 项目名称                                           |
| signup_start_time        | DATETIME      | 报名/竞价开始时间                                  |
| signup_end_time          | DATETIME      | 报名/竞价截止时间                                  |
| disposal_party           | VARCHAR(1000) | 处置方                                             |
| disposal_agency          | VARCHAR(1000) | 处置机构                                           |
| right_holder             | VARCHAR(1000) | 权利人、所有权人、产权人                           |
| start_price_amount       | DECIMAL(20,2) | 起拍价，单位元                                     |
| start_price_display      | VARCHAR(200)  | 起拍价展示值                                       |
| final_price_amount       | DECIMAL(20,2) | 采集时有效价，单位元                               |
| final_price_display      | VARCHAR(200)  | 有效价展示值                                       |
| price_basis              | VARCHAR(100)  | 有效价依据                                         |
| contact_info             | VARCHAR(2000) | 联系方式                                           |
| special_notice           | LONGTEXT      | 特别告知、重大提示、风险提示                       |
| disclosed_defects        | LONGTEXT      | 公示瑕疵、权利负担、风险瑕疵                       |
| assessment_price_amount  | DECIMAL(20,2) | 评估价格，单位元                                   |
| assessment_price_display | VARCHAR(200)  | 评估价格展示值                                     |
| assessment_price_basis   | VARCHAR(100)  | 评估价来源标签，如评估价、市场价、参考价、评估报告 |
| assessment_date          | DATE          | 评估基准日或评估日期                               |
| dedup_hash               | CHAR(64)      | 跨平台去重指纹                                     |
| first_seen_at            | DATETIME      | 首次发现时间                                       |
| last_seen_at             | DATETIME      | 最近发现时间                                       |
| last_crawled_at          | DATETIME      | 最近详情采集时间                                   |
| created_at               | DATETIME      | 入库时间                                           |
| updated_at               | DATETIME      | 更新时间                                           |

### 5.2 raw_payloads：原始数据归档表

一条标的可以有多条原始记录。

典型 `payload_type`：

- `list_json`
- `detail_json`
- `realtime_json`
- `announcement_html`
- `notice_html`
- `description_html`
- `location_html`
- `deposit_html`
- `attachment_list_json`
- `attachment_text`
- `browser_html`
- `platform_api_json`

重要字段：

| 字段            | 类型          | 说明          |
| --------------- | ------------- | ------------- |
| payload_id      | BIGINT PK     | 原始记录 ID   |
| item_id         | BIGINT        | 标的 ID       |
| batch_id        | VARCHAR(64)   | 批次 ID       |
| source_platform | VARCHAR(50)   | 来源平台      |
| payload_type    | VARCHAR(80)   | 原始数据类型  |
| source_url      | VARCHAR(1000) | 来源 URL      |
| source_tab      | VARCHAR(100)  | 页面标签页    |
| payload_text    | LONGTEXT      | HTML 或纯文本 |
| payload_json    | JSON          | JSON 原文     |
| payload_hash    | CHAR(64)      | 原文哈希      |
| fetched_at      | DATETIME      | 获取时间      |

### 5.3 field_extractions：字段提取证据表

这是数据准确性的核心表。

| 字段                | 类型          | 说明                                                 |
| ------------------- | ------------- | ---------------------------------------------------- |
| extraction_id       | BIGINT PK     | 提取记录 ID                                          |
| item_id             | BIGINT        | 标的 ID                                              |
| field_namespace     | VARCHAR(50)   | common、special、detail                              |
| asset_group         | VARCHAR(50)   | 资产类型                                             |
| field_key           | VARCHAR(100)  | 字段英文名                                           |
| field_label         | VARCHAR(200)  | 字段中文名                                           |
| display_value       | LONGTEXT      | 展示值                                               |
| normalized_text     | LONGTEXT      | 标准化文本值                                         |
| numeric_value       | DECIMAL(20,6) | 数值、金额、面积                                     |
| date_value          | DATE          | 日期值                                               |
| datetime_value      | DATETIME      | 时间值                                               |
| value_unit          | VARCHAR(50)   | 单位，如元、平方米                                   |
| method              | VARCHAR(80)   | api、html_regex、ai、derived、validation             |
| source_payload_id   | BIGINT        | 原始数据 ID                                          |
| source_payload_type | VARCHAR(80)   | 原始数据类型                                         |
| source_tab          | VARCHAR(100)  | 页面标签页                                           |
| source_path         | VARCHAR(500)  | JSON 路径或 HTML 定位                                |
| source_excerpt      | LONGTEXT      | 原文证据片段                                         |
| confidence          | DECIMAL(5,4)  | 置信度                                               |
| status              | VARCHAR(50)   | extracted、missing、conflict、rejected、needs_review |
| is_selected         | TINYINT       | 是否为最终采用值                                     |
| missing_reason      | VARCHAR(1000) | 缺失或拒绝原因                                       |
| created_at          | DATETIME      | 创建时间                                             |

字段值入主表前，必须在这里留下证据。

### 5.4 item_resources：附件和媒体表

| 字段                | 类型          | 说明                                                         |
| ------------------- | ------------- | ------------------------------------------------------------ |
| resource_id         | BIGINT PK     | 资源 ID                                                      |
| item_id             | BIGINT        | 标的 ID                                                      |
| resource_type       | VARCHAR(30)   | attachment、image、video                                     |
| resource_role       | VARCHAR(80)   | 资源角色，如 site_image、vehicle_image、subject_image、announcement_file、assessment_report、asset_list |
| resource_name       | VARCHAR(1000) | 文件名或图片说明                                             |
| resource_url        | VARCHAR(2000) | URL                                                          |
| resource_format     | VARCHAR(50)   | PDF、DOCX、JPG 等                                            |
| resource_size_bytes | BIGINT        | 文件大小                                                     |
| source_section      | VARCHAR(100)  | 来源区块                                                     |
| url_hash            | CHAR(64)      | URL 哈希                                                     |
| content_hash        | CHAR(64)      | 文件内容哈希，未下载时为空                                   |
| is_downloaded       | TINYINT       | 是否已下载                                                   |
| created_at          | DATETIME      | 创建时间                                                     |

现场图片、车辆图片、标的图片、公告文件、评估报告、清单附件都进入此表。附件数量和图片/视频数量通过 `item_resources` 实时统计，不在 `auction_items` 冗余存储。

## 6. 资产特有表

所有特有表都使用 `item_id` 作为主键并关联 `auction_items.item_id`。

### 6.1 asset_real_estate：房地产

| 字段                  | 类型          | 说明             |
| --------------------- | ------------- | ---------------- |
| right_certificate_no  | VARCHAR(500)  | 权证编号         |
| building_area_sqm     | DECIMAL(20,6) | 建筑面积，平方米 |
| building_area_display | VARCHAR(200)  | 建筑面积展示值   |
| property_use          | VARCHAR(500)  | 房产用途         |
| use_term              | VARCHAR(500)  | 使用年限         |
| property_location     | VARCHAR(1000) | 房产位置         |
| property_structure    | VARCHAR(500)  | 房产结构         |
| property_status       | VARCHAR(1000) | 房产状态         |
| property_type         | VARCHAR(500)  | 房产类型         |
| asset_highlights      | LONGTEXT      | 资产亮点         |

### 6.2 asset_land：土地

| 字段                 | 类型          | 说明             |
| -------------------- | ------------- | ---------------- |
| right_certificate_no | VARCHAR(500)  | 权证编号         |
| land_area_sqm        | DECIMAL(20,6) | 土地面积，平方米 |
| land_area_display    | VARCHAR(200)  | 土地面积展示值   |
| land_use             | VARCHAR(500)  | 土地用途         |
| use_term             | VARCHAR(500)  | 使用期限         |
| land_location        | VARCHAR(1000) | 土地位置         |
| land_status          | VARCHAR(1000) | 土地状态         |
| land_type            | VARCHAR(500)  | 土地类型         |

评估时间及价值使用主表 `assessment_*`，证据在 `field_extractions`。

### 6.3 asset_vehicle：车辆

| 字段                  | 类型          | 说明         |
| --------------------- | ------------- | ------------ |
| storage_location      | VARCHAR(1000) | 存放位置     |
| vehicle_brand_model   | VARCHAR(1000) | 车型品牌     |
| vehicle_usage         | LONGTEXT      | 车辆使用情况 |
| plate_number          | VARCHAR(100)  | 车牌号       |
| vehicle_configuration | LONGTEXT      | 车辆配置     |
| vehicle_status        | LONGTEXT      | 车辆状态     |
| vehicle_type          | VARCHAR(500)  | 车辆类型     |

### 6.4 asset_debt：债权汇总

| 字段                      | 类型          | 说明           |
| ------------------------- | ------------- | -------------- |
| main_debtor_name          | VARCHAR(1000) | 主债务人名称   |
| debtor_names              | LONGTEXT      | 债务人汇总     |
| creditor                  | VARCHAR(1000) | 债权人         |
| principal_balance_amount  | DECIMAL(20,2) | 本金余额，元   |
| principal_balance_display | VARCHAR(200)  | 本金余额展示值 |
| interest_balance_amount   | DECIMAL(20,2) | 利息余额，元   |
| interest_balance_display  | VARCHAR(200)  | 利息余额展示值 |
| claim_total_amount        | DECIMAL(20,2) | 债权总额，元   |
| claim_total_display       | VARCHAR(200)  | 债权总额展示值 |
| benchmark_date            | DATE          | 基准日         |
| guarantee_method          | VARCHAR(1000) | 担保方式       |
| guarantor                 | LONGTEXT      | 保证人         |
| collateral                | LONGTEXT      | 抵质押物       |
| litigation_status         | LONGTEXT      | 诉讼状态       |
| household_count           | INT           | 户数           |

### 6.5 asset_debt_details：债权逐户明细

债权包中每户一行。单户债权也写一行，方便统一查询。

| 字段                     | 类型          | 说明         |
| ------------------------ | ------------- | ------------ |
| debt_detail_id           | BIGINT PK     | 明细 ID      |
| item_id                  | BIGINT        | 标的 ID      |
| detail_index             | INT           | 明细顺序     |
| sequence_no              | VARCHAR(100)  | 原表序号     |
| debtor_name              | VARCHAR(1000) | 债务人       |
| principal_balance_amount | DECIMAL(20,2) | 本金余额，元 |
| interest_balance_amount  | DECIMAL(20,2) | 利息余额，元 |
| claim_total_amount       | DECIMAL(20,2) | 债权总额，元 |
| benchmark_date           | DATE          | 基准日       |
| guarantor                | LONGTEXT      | 保证人       |
| collateral               | LONGTEXT      | 抵质押物     |
| litigation_status        | LONGTEXT      | 诉讼状态     |
| source_payload_id        | BIGINT        | 来源原文 ID  |
| source_excerpt           | LONGTEXT      | 原文片段     |

债权逐户明细不再保存 `*_display` 字段；原始展示值、单位和证据片段统一进入 `field_extractions`。

### 6.6 asset_ip 与 asset_ip_details：知识产权

`asset_ip` 保存汇总字段：

- `subject_name`
- `ip_count`
- `certificate_no`
- `ip_type`
- `specific_category`
- `subject_intro`
- `right_term`

`asset_ip_details` 保存逐项明细：

- 软件名称、专利名称、作品名称。
- 证书号、登记号、申请号。
- 取得方式、申请日、登记批准日。
- 专利类型、权利状态、权利人。
- 原表序号和证据片段。

如果页面中存在图片表格，AI/OCR 识别结果也必须写入该表，并在 `field_extractions` 或 `raw_payloads` 中记录来源图片。

### 6.7 其他资产类型

设备、股权、物资产品、用益物权、其他类型按截图字段建独立表。原则是：

- 可计算、可筛选的字段用结构化类型。
- 类型特有的长文本用 `LONGTEXT`；公示瑕疵统一放在 `auction_items.disclosed_defects`。
- 无稳定结构的原文放 `asset_other.raw_detail_text`。

## 7. 跨平台去重

`asset_dedup_index` 每个标的一行。

`dedup_hash` 由以下字段标准化后生成：

- 资产类型。
- 项目名称。
- 所在地。
- 起拍价或有效价。
- 权证号、车牌号、债务人、标的企业、权利人、证书号等类型特有关键字段。

同一个 `dedup_hash` 出现多条数据时，不自动覆盖，而是：

- `duplicate_status='suspected'`
- `canonical_item_id` 指向建议主记录
- Viewer 提示“疑似重复”
- 人工审核后再设为 `confirmed` 或 `ignored`

## 8. 定时采集相关表

### 8.1 crawl_jobs

记录任务配置，例如：

- 每天 02:00 跑京东 5 页。
- 每天 03:00 跑 e交易债权 10 页。
- 每周跑一次阿里拍卖。

任务配置包含平台、类目、页数、限速、是否启用 AI、是否启用附件解析等。

### 8.2 crawl_job_runs

每次任务执行一行，记录成功数、失败数、耗时、错误消息。

### 8.3 crawl_queue

标的详情队列表：

- 列表页发现标的后写入队列。
- Worker 抢任务。
- 单条失败只影响该标的。
- `running` 超时后可回到 `pending`。
- `batch_id` 必填。同一批次内使用 `(source_platform, source_item_id, batch_id)` 去重，避免同一标的重复入队。

## 9. Viewer 展示原则

Viewer 不再直接展示数据库列名，而是优先读取：

- `field_catalog` 的中文名和备注。
- `auction_items` 的主字段。
- 对应资产类型表。
- `field_extractions` 的证据、置信度、缺失原因。
- `item_resources` 的附件、图片、视频。

每个字段展示四块信息：

- 字段名。
- 最终值。
- 状态：已提取、缺失、冲突、被过滤、需审核。
- 证据：来源标签页、来源路径、原文片段。

## 10. 迁移原则

后续从现有代码迁移到 V2 表结构时，建议分三步：

1. 新建 V2 表，不覆盖旧表。
2. 写入层同时支持旧表和 V2 表，先用小批量验证。
3. Viewer 切换到 V2 表后，再决定是否废弃旧表。

本地测试库中的旧测试数据可以删除重建，但正式环境建议保留迁移脚本和回滚方案。
# 新图片债权清洗抽取运行说明

## 1. 安装依赖

```powershell
cd F:\test_0608\ceshi_pic
.\.venv\Scripts\python.exe -m pip install -r .\requirements.txt
```

如果没有使用虚拟环境，也可以用当前 Python 执行相同命令。

## 2. 配置环境变量

不要把密钥写进代码或日志。按实际供应商配置：

```powershell
$env:ARK_CODING_API_KEY="..."
$env:ARK_CODING_BASE_URL="https://ark.cn-beijing.volces.com/api/coding/v3"
$env:ARK_CODING_MODEL="..."
```

MySQL staging 入库前再配置：

```powershell
$env:MYSQL_HOST="..."
$env:MYSQL_PORT="3306"
$env:MYSQL_USER="..."
$env:MYSQL_PASSWORD="..."
$env:MYSQL_DATABASE="..."
```

## 3. 标准流程

新 `image/` 文件夹默认走整图优先，每张图片作为一个 block。

```powershell
.\.venv\Scripts\python.exe .\credit_claim_skill.py prepare --input-dir .\image --output-dir .\outputs_image_whole
.\.venv\Scripts\python.exe .\credit_claim_skill.py smoke-test --input-dir .\image --output-dir .\outputs_image_whole_smoke --provider auto
.\.venv\Scripts\python.exe .\credit_claim_skill.py extract --input-dir .\image --output-dir .\outputs_image_whole --provider auto --smoke-first
.\.venv\Scripts\python.exe .\credit_claim_skill.py audit --output-dir .\outputs_image_whole --sample-size 8
```

主要输出：

- `债权清洗结果.csv`：12 个正式业务字段。
- `债权清洗结果_带来源.csv`：含来源图片、bbox、切块图、模型、明细序号、置信度、原文摘录。
- `清洗结果问题清单.csv`：字段过少、金额口径异常、低置信度、角色冲突等问题。
- `复核清单.csv`：模型调用或空结果等 block 级复核项。
- `抽检报告.md`、`随机抽检样本.csv`：随机抽检交付物。

## 4. MySQL Staging

先连通性测试：

```powershell
.\.venv\Scripts\python.exe .\credit_claim_skill.py load-db --check
```

只读取 CSV、不写库：

```powershell
.\.venv\Scripts\python.exe .\credit_claim_skill.py load-db --output-dir .\outputs_image_whole --dry-run
```

确认库名、权限和 CSV 结果后写入 staging：

```powershell
.\.venv\Scripts\python.exe .\credit_claim_skill.py load-db --output-dir .\outputs_image_whole --input-dir .\image
```

入库表：

- `credit_claim_runs`
- `credit_claim_records_staging`
- `credit_claim_issues`

## 5. 兜底策略

- 火山方舟 CodingPlan 优先，但必须先 smoke test 验证是否支持图片输入和 JSON 输出。
- 如果大表 JSON 被截断，优先提高 `--max-tokens`；仍失败时再按长图/表格方向分段处理。
- 如果发现错位、字段过少、金额口径不确定，不直接猜填，进入问题清单和人工复核。
